# Database (PostgreSQL — Neon recommended: https://neon.tech)
DATABASE_URL=postgresql://user:pass@host/skillshub?sslmode=require

# GitHub OAuth App (https://github.com/settings/developers)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=http://localhost:3000/callback

# Session secret (min 32 chars)
# Generate: openssl rand -base64 32
SESSION_SECRET=your-session-secret-at-least-32-characters

# Encryption key for GitHub token storage (64 hex chars = 32 bytes)
# Generate: openssl rand -hex 32
ENCRYPTION_KEY=your-64-hex-char-encryption-key

# Platform BSC address for 5% donation fee
NEXT_PUBLIC_PLATFORM_BSC_ADDRESS=0x0000000000000000000000000000000000000000

# App URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3001

# Optional: GitHub token for seeder (higher rate limits)
# GITHUB_TOKEN=ghp_...

# Optional: BSC RPC endpoint
# BSC_RPC_URL=https://bsc-dataseed.binance.org
